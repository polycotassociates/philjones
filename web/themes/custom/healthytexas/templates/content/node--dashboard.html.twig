{% set classes = ['dashboard'] %}
{# {{vardumper(content.field_user)}}
 #}
{# {{vardumper()}} #}

{# <p>
	name:
	{{content.field_user.0["#title"]}}
	<br/>
	participant url:
	{{content.field_user.0["#url"]}}
	<br/>
	participant title:
	{{content.field_user["#title"]}}
	<br/>
	participant id:
	{{content.field_response_id}}
	<br/>
	url:
	{{url}}
	<br/>
	12weeks:
	{{elements.field_timepoint_12w}}
	<br/>
	baseline:
	{{elements.field_timepoint_baseline}}
	<br/>
	programtype:
{{elements.field_program_type}}
</p> #}

{# <div class="programItem">
  <div class="programItem--username">
    {{content.field_user}}
  </div>
    <div class="panel panel-default col-sm-3">
      <div class="panel-heading">
        <div class="panel-title">
          Timepoints
        </div>
      </div>
      <div class="panel-body">
        {{elements.field_timepoint_baseline}}
        {{elements.field_timepoint_12w}}
      </div>
    </div>
  </div>
</div> #}

	<div class="panel panel-default dashboard"> <div class="dashboard__title">
		{{elements.field_program_type}}
	</div>

	<div class="dashboard__participant">
		{{content.field_user}}
	</div>
	<div class="panel panel-default timepoints dashboard__timepoints">
		<div class="panel-heading">
			<div class="panel-title">
				Timepoints
			</div>
		</div>
		<div class="panel-body">
			{{elements.field_timepoint_baseline}}
			{{elements.field_timepoint_12w}}
		</div>
	</div>
</div>
